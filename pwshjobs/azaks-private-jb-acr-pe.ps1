# Creates private AKS
# Create Azure VM running ubuntu in the same VNET different subnet
# Configure Docker inside the VM
# Create ACR with public access disabled
# Import example image to ACR - acr import
# Configure private endpoint connection from ACR to AKS VNET
# Attach ACR to AKS cluster
# Find a way to run docker login or suggest validated commands to user
# Suggest user on aks get-credentials to connect to the private cluster from VM
