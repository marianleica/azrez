# Creates AKS
# Create ACR
# Import example image to ACR - acr import
# Attach ACR to AKS cluster
# Test pod pull image on AKS from ACR using az aks invoke commands
# Lastly, run az aks get-credentials --overwrite --admin or provide it to the user
